load("//:index.bzl", "karma_test")

karma_test(
    name = "example",
    deps = glob(["specs/*.js", "adders/*.js"]),
    # deps = ["bundle.spec.js"],
    #deps = glob(["thing*.js"]),
    tags = ["local"]
)
